<!DOCTYPE html>
<html>
    <head>
        <link rel="stylesheet" href="./css/style.css">
    </head>
    <body>
        <h1>{{>header}}</h1>
        <p>Error type: {{error}}</p>
        <p>Footer: </p>
        <h1>{{>footer}}</h1>
    </body>
</html>